{% extends 'base.html.twig' %}

{% block body %}

    <h1>Documents creation</h1>

    <div class="container">
        <div class="row">
            <div class="col m8 offset-m2">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <input type="submit" value="Create" />
                {{ form_end(form) }}

                <ul>
                    <li>
                        <a href="{{ path('documents_index') }}">Back to the list</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>

{% endblock %}
